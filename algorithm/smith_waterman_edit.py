# initial the matrix by long seq1 * seq2 in 2d matrix
# def initial_matrix(seq_1, seq_2):
#     matrix = [[(len(seq_1) + 1) * [0]] for _ in range(len(seq_2) + 1)]
#     return matrix
#
#
# def scoring_matrix(line, col, matrix):
#     for i in range(3):
#
#     return None
#
#
#
# # def calculate_matrix(matrix, seq1, seq2, W):
# #     for i in range(len(matrix)):
# #         if i > 0:
# #             for j in range(len(matrix[i])):
# #                 if j > 0:
# #                     if seq1[j] == seq2[j]:
# #                         matrix[i][j] = scoring_matrix(i.j)
#
#
# for line in initial_matrix("AACCTTGG", "GGTTCC"):
#     print(line)
#
# print(len("AACCTTGG") + 1, len("GGTTCC") + 1)
